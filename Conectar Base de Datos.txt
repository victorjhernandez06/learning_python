### -- Forma 1: -- ###
## debemos instalar el pip install mysql-connector-python.
## importamos:  import mysql.connector

import mysql.connector
cnn = mysql.connector.connect(
    host = "localhost",
    user = "root",
    passwd = "",
    database = "world"
)
print(cnn)

### -- Forma 2: -- ###
## instalamos pip install pymysql
## importamos: import pymysql

import pymysql
miConexion = pymysql.connect(
    host = "localhost",
    user = "root",
    passwd = "",
    database = "world"
)
print(x)

### -- Forma 3: -- ###
## instalamos pip install MySQL-python
## importamos: import MySQLdb

import MySQLdb
cnn = MySQLdb.connet(
    host = "localhost",
    user = "root",
    passwd = "",
    database = "world"
)

### -- Forma 4: -- ###
## importamos: mysql.connector
## Creamos una funcion para conectar a la base de datos.
##

import mysql.connector
from mysql.connector import Error  # ==> revisa la conexion remota.import

## --- Creamos la funcion ---- ##
def conectar():
    try:
        conexion = mysql.connector.connect(
            host='localhost',  # ==> si tienes una remota, aqui va la ruta.
            user='root',
            password="",
            database='datospersonales'
        )
        return conexion
    except Error as e:
        messagebox.showerror('Error', str(e))


### la conexion la hacemos casi igual en todas las formas o maneras.
